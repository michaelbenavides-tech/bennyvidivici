services:
  backend:
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
  frontend:
    build:
      context: ./frontend
      target: build
    command: npm run dev
    volumes:
      - ./frontend:/app
      - /app/node_modules
